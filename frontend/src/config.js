// API Configuration
// Change this before building for production
export const API_CONFIG = {
  // Development: uses localhost
  // Production: change to your EC2 URL before building
  DEFAULT_API_URL: "http://localhost:4000/chat"
  
  // For EC2 deployment, change to:
  // DEFAULT_API_URL: "http://your-ec2-ip:4000/chat"
  // or
  // DEFAULT_API_URL: "https://api.yourdomain.com/chat"
};
